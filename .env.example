DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require&channel_binding=require"

NEXT_PUBLIC_CONTACT_NAME="Sohail Siddiqui"
NEXT_PUBLIC_CONTACT_PHONE="+919892587749"
NEXT_PUBLIC_SITE_URL="https://your-domain.com"

# --- Admin dashboard (/admin) auth ---
SESSION_SECRET="replace-with-a-random-32-byte-base64-string"
ADMIN_PASSWORD="replace-with-a-strong-password"

# --- Meta Ads: Marketing API (OAuth + ad account sync) ---
META_APP_ID=""
META_APP_SECRET=""
META_REDIRECT_URI="https://your-domain.com/api/meta/oauth/callback"
META_GRAPH_API_VERSION="v21.0"

# --- Meta Pixel + Conversions API ---
# All three below can also be set from /admin/meta-capi (stored in the database and
# take priority over these), so they're optional fallbacks rather than required.
META_PIXEL_ID=""
NEXT_PUBLIC_META_PIXEL_ID=""
META_CAPI_ACCESS_TOKEN=""
META_CAPI_TEST_EVENT_CODE=""

# --- Telegram lead notifications (optional — can also be set from /admin/telegram) ---
TELEGRAM_BOT_TOKEN=""
TELEGRAM_CHAT_ID=""

# Protects /api/cron/meta-sync
CRON_SECRET="replace-with-a-random-32-byte-base64-string"
